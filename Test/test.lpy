import sys
sys.path.append("../RiemannTurtleLib")
from math import *
import numpy as np

%pastefile ../RiemannTurtleLib/riemannianrules.lpy

Axiom: 
  nproduce SetSpace(Sphere()) InitTurtle(0.5,0.5,0.5,0.5)
  nb = 10
  u0,v0 = 0.5,0.5
  a = 0
  for i in range(nb):  
    a += radians(360)/nb
    nproduce [InitTurtle([u0,v0,np.cos(np.pi/2+a),np.sin(np.pi/2+a)]) InterpolateColors(3,5, i/(nb-1)) A]

derivation length: 1
production:

A --> F(1)

interpretation:


endlsystem
